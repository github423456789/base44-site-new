[build]
  command = "npm run build"
  publish = "dist/sport-stream-finder-f1b78b6c" # This is the main fix

[functions]
  directory = "src/api/funtions"

# This rule is essential for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
